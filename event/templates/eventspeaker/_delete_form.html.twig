<form method="post" action="{{ path('app_eventspeaker_delete', {'id': eventspeaker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventspeaker.id) }}">
    <button class="btn">Delete</button>
</form>
