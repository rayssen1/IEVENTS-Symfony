{% extends 'evenement/base-back.html.twig' %}

{% block title %}Modifier un Événement{% endblock %}

{% block body %}
<div class="container py-4">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Événements /</span> Modifier
    </h4>

    <div class="card mb-4">
        <div class="card-body">
            {{ include('evenement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_evenement_index') }}" class="btn btn-outline-secondary">
            <i class="bx bx-arrow-back me-1"></i> Retour à la liste
        </a>

        {{ include('evenement/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
