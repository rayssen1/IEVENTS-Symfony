{% extends 'evenement/base-back.html.twig' %}

{% block title %}Modifier un Eventspeaker{% endblock %}

{% block body %}
<div class="container-xxl flex-grow-1 container-p-y">
  <h4 class="fw-bold py-3 mb-4">
    <span class="text-muted fw-light">Eventspeakers /</span> Modifier
  </h4>

  <div class="row">
    <div class="col-md-12">
      <div class="card mb-4">
        <h5 class="card-header">Formulaire de modification</h5>
        <div class="card-body">
          {{ include('eventspeaker/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
      </div>

      <a href="{{ path('app_eventspeaker_index') }}" class="btn btn-outline-secondary">
        <i class="bx bx-arrow-back me-1"></i> Retour à la liste
      </a>
    </div>
  </div>
</div>
{% endblock %}